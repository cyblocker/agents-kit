{
  "name": "agents-kit",
  "version": "1.0.0",
  "description": "Ingress Season Planner",
  "scripts": {
    "build": "npx @tailwindcss/cli -i style.css -o utilities.css --minify && node build.js",
    "dev": "npm run build && npx http-server dist"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "terser": "^5.29.0"
  }
}
